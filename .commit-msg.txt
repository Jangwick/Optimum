feat: replace row-click navigation with View action button in Claims

Removed the clickable-row behavior from the Claims registry table.
Added a formal "View" action button in a new Actions column at the
right of each row.

- Removed onRowClick from DataTable usage (rows no longer have
  cursor-pointer or click handlers)
- Added rowActions prop with a "View" button (Eye icon) that
  navigates to the claim detail page
- Button has hover state (fills with primary color) and tooltip
- Uses stopPropagation to avoid any parent click handlers
- DataTable already supported rowActions — just needed to use it

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
