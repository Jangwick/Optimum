feat: reflect all Claim Detail actions in Summary tab and activity history

Backend:
- Add recordActivity() helper in activity.service.js for system-generated activities
- Auto-sync financial fields to Claim record: assessment total -> estimatedLoss/reserve,
  settlement/offer -> proposedSettlement/agreedSettlement, fee total -> actualLoss
- Create ClaimActivity records for all tab mutations (assessment, settlement, fee,
  invoice, payment, document, investigation, inspection, contact, report, clarification,
  task, insurer panel)
- Enhance getClaim() response with financials object (assessmentTotal, feeTotal,
  invoiceTotal, paymentTotal, documentCount, taskCount, openTaskCount)
- Increase activity limit from 20 to 50
- Update controllers to pass req.user.id to service functions

Frontend:
- Pass onClaimChange callback to all tab components, triggered after every mutation
- Reload claim data on tab switch so Summary/Timeline always show fresh data
- Redesign Summary tab: replace Status History with compact Recent Activity timeline
  (merges processHistory + activities + correspondence, shows latest 12 events)
- Add financial summary rows: Assessment Total, Fee Total, Invoice Total, Payment Total
- Add count badges: Documents, Open/Total Tasks, Process Status

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
