feat: add delete buttons to investigations, contacts, and inspections

Added delete functionality to all three sub-tabs in ClaimInvestigation:

- Investigations: delete button with Trash2 icon on each investigation card
- Contacts: delete button with Trash2 icon on each contact card
- Inspections: delete button with Trash2 icon on each inspection card
  (confirms deletion of inspection and all its photos)

Added frontend service functions:
- deleteInvestigation(claimId, id)
- deleteContact(claimId, id)
- deleteInspection(claimId, id)

All delete buttons use confirm() dialog before deletion and refresh
the list after successful deletion.

Verified in browser: deleted a stale inspection record successfully.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
