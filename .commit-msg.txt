feat: redesign TimelineTab with unified events, filters, and polished UX

Complete rewrite of TimelineTab in ClaimDetail:

Summary cards (always visible):
- Total Events (count of all events)
- Activities (count of activity events)
- Correspondence (count of correspondence events)
- Status Changes (count of process status changes)

Unified timeline:
- Combines three event sources into one chronological view:
  1. Activities (from claim.activities)
  2. Correspondence (from claim.correspondence)
  3. Status Changes (from claim.processHistory) — previously missing
- All events sorted by date (newest first)
- Each event has a unique ID prefix (a-, c-, s-) for proper React keys

Filter tabs:
- All Events (total count)
- Activities (count)
- Correspondence (count)
- Status Changes (count)
- Active filter highlighted with primary background
- Count badges on each filter tab
- Empty state adapts to selected filter

Timeline visual design:
- Card with primary left border and header
- Vertical timeline with icon badges:
  - Activities: GitBranch icon, primary color
  - Correspondence: FileText icon, secondary color
  - Status Changes: CheckCircle icon, success color
- Connecting line between events (hidden on last item)
- Each event in a rounded card with:
  - Type badge (color-coded)
  - Event title
  - Historical badge (for historical correspondence)
  - Description
  - Metadata: date, actor, source, follow-up date
- Empty state with icon and contextual hint

Verified in browser:
1. Timeline shows 1 status change event (Claim Assigned)
   - Total Events: 1, Activities: 0, Correspondence: 0, Status Changes: 1
2. Filter tabs work:
   - All Events: shows 1 event
   - Activities: shows empty state "No activity events recorded"
   - Status Changes: shows 1 event
3. Event card displays: status badge, title, description, date, actor
4. No JS errors
5. Accessibility warnings reduced from 12+11 to 2+2

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
