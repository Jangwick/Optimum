fix: make Upload Photo button trigger file picker directly

Removed the visible file input and "Choose File" button. Now the
"Upload Photo" button itself triggers the hidden file input via ref,
and the selected file is uploaded immediately on selection.

- Hidden file input with ref keyed by inspection ID
- Button click calls triggerFileInput() which clicks the hidden input
- handleFileSelect() uploads the file immediately after selection
- Removed the photos state (no longer needed for two-step flow)
- Removed the disabled state on the button (always enabled now)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
