feat: complete ClaimDetail feature audit and fix all gaps

Audit findings and fixes across all 11 tabs:

1. Insurer Panel tab — was read-only display only, no CRUD UI
   - Added "Add Insurer" button with full form (insurance company,
     participation %, insurer claim #, lead checkbox, notes)
   - Added toggle lead insurer button (click Yes/No to toggle)
   - Added remove insurer button with confirmation
   - Added insurer panel CRUD service functions to claim.service.js
   - Panel reloads after add/remove/toggle via getClaim()

2. Tasks and Finance tabs — getUsers() calls GET /users which requires
   ADMIN role, would 403 for Engineer/Accountant users
   - Wrapped getUsers() in .catch() to gracefully degrade to empty array
   - Disabled user dropdowns with "No users available" when empty
   - Applied to both TasksTab and ClaimFinance

3. Process Status tab — inconsistent styling with rest of claim detail
   - Replaced old amber-50/amber-300 alert with design-system tokens
     (accent-orange/5, accent-orange/30) and Lock icon
   - Added labeled form fields (New Status, Notes) with focus rings
   - Updated current status display to use pill with dot indicator
   - Updated Process History to use timeline style (dots + connecting
     lines) matching Status History
   - Added GitBranch icon to Update Process Status button

4. Timeline tab — old border-l-2 style
   - Updated to use consistent timeline style with dots and connecting
     lines matching Status History and Process History

5. Visual consistency — all section containers
   - Replaced remaining "rounded shadow-sm" with "rounded-lg shadow-sm"
     across ClaimDetail, ClaimInvestigation, and ClaimFinance

Verification:
- Client lint: 0 errors
- Client build: success
- Server lint: 0 errors
- Server tests: 47 passed, 5 suites
- Browser: all 11 tabs verified via Chrome DevTools
- No JS console errors

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
