feat: add photo viewer modal for inspection photos

Clicking an inspection photo now opens a full-screen modal showing
the image at full size with download and close actions.

Backend:
- Added GET /api/claims/:claimId/inspections/photos/:photoId route
  that streams the photo file with correct Content-Type
- Added getPhoto service and getInspectionPhoto controller

Frontend:
- Photo thumbnails now render as actual <img> elements (with Camera
  icon fallback on error) instead of just a placeholder icon
- Each photo thumbnail is a clickable button that opens the modal
- Photo viewer modal features:
  - Full-size image display (object-contain, max 65vh height)
  - Header with filename and caption
  - Download button (downloads original file)
  - Close button (X icon)
  - Click outside or ESC-style backdrop to close
  - Backdrop overlay (bg-black/80)
  - Stop propagation on modal content to prevent accidental close

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
