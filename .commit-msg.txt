feat: redesign DocumentsTab file input as styled dropzone

Replaces the native file input button with a polished drag-and-drop
dropzone that matches the design system.

Before: native <input type="file"> with styled file::button pseudo-element
After: custom dropzone with hidden file input

Features:
- Dashed border dropzone area with UploadCloud icon
- Click to browse (triggers hidden file input)
- Drag and drop support (dragover/dragleave/drop events)
- Visual feedback when dragging (primary border + bg tint)
- File type hint text (PDF, DOC, DOCX, XLS, XLSX, JPG, PNG)
- When file selected, shows a file card with:
  - File icon badge
  - File name (truncated)
  - File size in KB
  - Remove (X) button to clear selection
- Hidden native input preserved for form validation
- Category and Description fields span below the dropzone
- Description field now spans 2 columns for more space

Verified in browser:
1. Documents tab shows new dropzone with "Click to browse or drag a
   file here" text and file type hint
2. Clicking dropzone opens native file picker
3. No JS errors

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
