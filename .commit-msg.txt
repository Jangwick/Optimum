feat: redesign DocumentsTab with full CRUD, download, and polished UX

Complete rewrite of DocumentsTab in ClaimDetail plus backend fix:

Summary cards (always visible):
- Total Documents (count of all uploaded docs)
- Received (count of docs marked as received)
- Pending (count of docs not yet received)

Upload form:
- Card with primary left border and header
- File input with styled "Choose File" button
- Category dropdown (10 categories) with label
- Description input with label
- Upload button with icon, loading state, disabled until
  file and category are selected
- Form resets after successful upload (including file input)

Document checklist:
- Category sections with header showing:
  - Category name with folder icon
  - "Required" badge for required categories
  - File count and received count
- Document cards showing:
  - File-type icon (image/pdf/generic)
  - Original filename
  - "Received" badge (green) when marked received
  - Description, file size, upload date, uploaded by user
  - Download button (downloads original file via blob)
  - Mark Received button (hidden when already received)
  - Delete button with confirmation dialog
- Empty state when no categories configured
- "No documents uploaded yet" per category

Backend fix (document.service.js):
- getDocumentChecklist now returns categories from BOTH
  claim type requirements AND uploaded documents
- Previously only showed required categories, so documents
  uploaded to non-required categories were invisible
- Also handles uncategorized documents

State management:
- Loading state during fetch
- Uploading state during upload
- Error banner with dismiss button
- Confirmation dialog before delete
- File input ref for proper reset

Verified in browser:
1. Uploaded test-doc.txt to Policy category with description
   - Total Documents: 1, Pending: 1
   - Document card with file icon, size (21 B), date, user
2. Marked as received
   - Received: 1, Pending: 0
   - "Received" badge appeared
   - Mark Received button hidden
3. Download and Delete buttons functional

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
