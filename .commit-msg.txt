feat: redesign ReportsTab with full CRUD, generation, clarifications, and polished UX

Complete rewrite of ReportsTab in ClaimDetail plus backend fix:

Summary cards (always visible):
- Total Reports (count of all reports)
- Drafts (count of DRAFT status)
- Submitted (count of non-DRAFT status)

Create Draft form:
- Card with primary left border and header
- Report Title input with label (required)
- Template dropdown with label (Default HTML + custom templates)
- Notes/Summary textarea with label
- Create Draft button with icon, loading state, disabled until
  title is entered
- Form resets after successful creation

Report cards:
- Card with primary left border
- Header: icon badge, title, created/generated dates, generated by
  user, status pill with semantic colors:
  DRAFT=neutral, SUBMITTED=primary, UNDER_REVIEW=orange,
  APPROVED=success, REJECTED=error, CLARIFICATION_REQUESTED=secondary
- Summary section with notes
- Generate Report button (only for DRAFT status) with loading state
- Download PDF link (when pdfPath exists) using authenticated URL
- Download DOCX link (when docxPath exists) using authenticated URL
- Request Clarification section (only for non-DRAFT reports):
  - Question input with Ask button
  - Disabled until question text is entered
- Clarifications list:
  - Status badge (ASKED=orange, ANSWERED=success)
  - Date and asked-by user
  - Q: question text
  - A: answer text (when answered) with answered-by user
- Versions list:
  - Version number, generation timestamp
  - Download PDF link per version

Empty state when no reports exist.
Loading state during fetch.
Error banner with dismiss button.

Backend fix (report.service.js):
- listReports now includes clarifications with askedBy and answeredBy
  relations, ordered by createdAt desc
- Previously clarifications were not included in the response

Frontend service (report.service.js):
- Added getDownloadUrl helper for building authenticated download URLs

Verified in browser:
1. Created report draft "Initial Damage Assessment Report"
   - Total: 1, Drafts: 1, DRAFT status pill
2. Generated report via Puppeteer
   - Status changed to SUBMITTED, Drafts: 0, Submitted: 1
   - Download PDF link appeared
   - Versions section: Version 1 with timestamp
   - Request Clarification section appeared
3. Sent clarification "Please provide photos of the damaged area"
   - Clarifications section: ASKED badge, date, user, Q: text
   - Form reset after sending

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
