feat: redesign SettlementTab with full CRUD, offers, and polished UX

Complete rewrite of SettlementTab in ClaimDetail:

Summary cards (always visible):
- Settled amount (from saved settlement)
- Offers count
- Accepted amount (from accepted offer, if any)
- Status (current settlement status)

Settlement form (left column):
- Card with primary left border and header showing status pill
- Settlement Date input with label
- Settled Amount input with label, min=0, monospace
- Status dropdown (Pending/Agreed/Rejected) with label
- Notes textarea with label
- Save Settlement button with icon and loading state
- Form pre-populates from existing settlement (upsert behavior)

New Offer form (left column):
- Card with accent-orange left border
- Offered Amount input with label, min=0, monospace
- Notes textarea with label
- Create Offer button with accent-orange color and loading state
- Form resets after successful creation

Offers list (right column):
- Card with header showing offer count
- Each offer card shows:
  - Header: icon badge, offered amount, date, created by, status pill
  - Status pills with semantic colors:
    PENDING=orange, ACCEPTED=success, REJECTED=error, COUNTERED=primary
  - Notes section with label
  - Response info section (when status != PENDING):
    shows response date and response by user
  - Response form (only for PENDING offers):
    - Status dropdown (Accept/Reject/Counter)
    - Response notes input
    - Submit Response button with CheckCircle icon
- Empty state with icon and hint
- Loading state during fetch

State management:
- Loading state during fetch
- Saving state during save/offer/response
- Error banner with AlertTriangle icon
- Form validation (require offered amount, require response status)
- Form reset after successful save

Verified in browser:
1. Saved settlement (₱15,000, AGREED status)
   - Settled card updated to ₱15,000
   - Status card updated to AGREED
   - AGREED status pill in settlement header
2. Created offer (₱12,000, "Initial offer based on assessment")
   - Offers count updated to 1
   - Offer card with PENDING status pill
   - Response form visible
3. Responded to offer (ACCEPTED, "Offer accepted by client")
   - Accepted card updated to ₱12,000
   - Offer status changed to ACCEPTED (green pill)
   - Response section showing date and user
   - Response form hidden after response

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
