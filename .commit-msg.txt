feat: convert New Claim from page navigation to modal dialog

Replaces the /claims/new page navigation with a modal dialog on both
the Claims Registry and Dashboard pages.

New component (NewClaimModal.jsx):
- Reusable modal built on the existing Modal component (size: xl)
- Two modes preserved: From Policy and Record Assignment
- All fields from the original page retained:
  - Policy mode: policy select, date of loss, location, estimated loss,
    reserve, description, engineer, accountant
  - Assignment mode: OCS ref, insurer claim #, assigned by, handling
    adjuster, insured name, client, insurer, claim type, policy details,
    nature of loss, claimed amount, estimated loss, reserve, engineer,
    accountant
- Loading state for reference data
- Saving state on submit button (disabled while creating)
- Error banner for API failures
- Form resets on close or successful creation
- onCreated callback navigates to the new claim detail page
- Labels on all fields (uppercase, label-md style)
- Focus styles consistent with design system

Claims.jsx changes:
- New Claim button now opens modal instead of navigating
- Empty state "New Claim" button also opens modal
- Modal rendered at end of page with onCreated → navigate to claim

Dashboard.jsx changes:
- New Claim button now opens modal instead of navigating
- Modal rendered at end of page with onCreated → navigate to claim

NewClaim.jsx:
- Left in place for backward compatibility (route still exists)

Verified in browser:
1. Clicked "New Claim" on Claims Registry → modal opened
2. Switched to "Record Assignment" mode → all Excel-style fields shown
3. Filled form: OCS-TEST123/MDL, Modal Test Insured, date 2026-08-15,
   client XYZ Retail, insurer Global Insurance, claim type Property
   Damage, engineer Field Engineer, accountant Senior Accountant
4. Clicked "Create Claim" → navigated to /claims/5
5. New claim detail page shows OCS-TEST123/MDL with correct data
6. No JS errors

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
