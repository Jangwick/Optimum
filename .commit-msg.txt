feat: redesign ClaimFinance with full CRUD, payments, and polished UX

Complete rewrite of ClaimFinance component (Fees + Invoices tabs):

Summary cards (always visible):
- Total Fees (sum of all fee amounts)
- Invoiced (sum of all invoice totals)
- Collected (sum of all payments received)

Fees tab:
- Fee Type dropdown with 7 predefined types (INSPECTION,
  INVESTIGATION, ASSESSMENT, REPORT, TRAVEL, CONSULTATION, OTHER)
  instead of free-text input
- Amount input with min=0, step=0.01, monospace font
- Description input with label
- Linked User dropdown with label
- Add Fee button with Plus icon and loading state
- Fee cards with primary left border, icon badge, fee type,
  linked user, amount, and "Invoiced" badge when applicable
- Empty state with icon and hint
- Loading state during fetch
- Error banner on failures

Invoices tab:
- Generate Invoice form with:
  - Unbilled fees checklist (only shows fees not yet invoiced)
  - Selected total calculation (live)
  - Due Date and Notes inputs with labels
  - Generate button disabled when no unbilled fees or none selected
  - "No unbilled fees available" message when applicable
- Invoice cards with:
  - Header: invoice number, issue/due dates, created by, status pill
  - Status pills using semantic colors (ISSUED=orange, PARTIAL=primary,
    PAID=success, OVERDUE=error) instead of inline styles
  - Fees section listing all billed fees
  - Payment summary grid: Total, Paid, Balance
  - Payment History section with all recorded payments
  - Record Payment form (hidden when PAID) with Amount, Date,
    Method, Reference inputs in a responsive grid
  - Record Payment button with DollarSign icon and success color
- Empty state with icon and hint

State management:
- Loading state during fetch
- Saving state during create/payment
- Error state with AlertTriangle banner
- Form validation (require fee selection for invoices)
- Form reset after successful save

Verified in browser:
- Created fee (INSPECTION, ₱5,000, Site inspection fee)
- Total Fees updated to ₱5,000
- Generated invoice from unbilled fee
- Invoiced updated to ₱5,000
- Recorded payment (₱5,000, Bank Transfer, REF-001)
- Collected updated to ₱5,000
- Invoice status changed to PAID
- Payment history displayed correctly
- Record Payment form hidden after full payment

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
