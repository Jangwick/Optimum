{
  "name": "claims-solutions-adjustment",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Claims Solutions Insurance Adjustment Management System",
  "scripts": {
    "dev": "concurrently -k -n 'SERVER,CLIENT' -c 'cyan,magenta' \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd client && npm run build",
    "build:full": "cd server && npx prisma generate && cd ../client && npm run build",
    "start": "cd server && node src/server.js",
    "start:prod": "cd server && npx prisma migrate deploy && npx prisma db seed && node src/server.js",
    "test": "cd server && npm test && cd ../client && npm test",
    "lint": "cd server && npm run lint && cd ../client && npm run lint",
    "format": "cd server && npm run format && cd ../client && npm run format"
  },
  "dependencies": {
    "@prisma/client": "7.9.1",
    "bcrypt": "6.0.0",
    "cookie-parser": "1.4.7",
    "cors": "2.8.6",
    "date-fns": "4.4.0",
    "docxtemplater": "3.69.3",
    "dotenv": "17.4.2",
    "exceljs": "4.4.0",
    "express": "4.22.2",
    "express-rate-limit": "8.6.2",
    "helmet": "8.3.0",
    "jsonwebtoken": "9.0.3",
    "multer": "2.2.0",
    "nanoid": "6.0.1",
    "pdfkit": "0.19.1",
    "pino": "10.3.1",
    "pizzip": "^3.2.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "concurrently": "10.0.4",
    "prisma": "7.9.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
