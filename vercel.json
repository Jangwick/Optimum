{
  "buildCommand": "npm install --legacy-peer-deps && DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy npx prisma generate --schema=server/prisma/schema.prisma && cd client && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "includeFiles": "server/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
